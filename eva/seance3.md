1. Faire toutes les pages du MVP (l'ensemble des pages permettant de faire fonctionner le site à minima) (contrôleur + views) Ne pas hésiter à utiliser bootstrap
2. Poursuivre MYSQL sur OpenClassroom
